apiVersion: v1
kind: Service
metadata:
  name: tarantool
  namespace: minitoolstream_connector
  labels:
    app: tarantool
spec:
  ports:
  - port: 3301
    targetPort: 3301
    protocol: TCP
    name: tarantool
  selector:
    app: tarantool
  type: ClusterIP
  clusterIP: None  # Headless service for StatefulSet
---
apiVersion: v1
kind: Service
metadata:
  name: tarantool-external
  namespace: minitoolstream_connector
  labels:
    app: tarantool
spec:
  ports:
  - port: 3301
    targetPort: 3301
    protocol: TCP
    name: tarantool
  selector:
    app: tarantool
  type: ClusterIP
