apiVersion: v1
kind: Secret
metadata:
  name: minitoolstream-egress-secret
  namespace: minitoolstream
  labels:
    app: minitoolstream-egress
    component: secret
type: Opaque
stringData:
  # Tarantool credentials
  TARANTOOL_PASSWORD: "changeme"

  # MinIO credentials
  MINIO_ACCESS_KEY_ID: "minioadmin"
  MINIO_SECRET_ACCESS_KEY: "minioadmin"

  # Vault token (if Vault is enabled)
  VAULT_TOKEN: ""
---
# Example: Using external secret operator (commented out)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: minitoolstream-egress-external-secret
#   namespace: minitoolstream
# spec:
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: minitoolstream-egress-secret
#     creationPolicy: Owner
#   data:
#     - secretKey: TARANTOOL_PASSWORD
#       remoteRef:
#         key: secret/data/minitoolstream/tarantool
#         property: password
#     - secretKey: MINIO_ACCESS_KEY_ID
#       remoteRef:
#         key: secret/data/minitoolstream/minio
#         property: access_key_id
#     - secretKey: MINIO_SECRET_ACCESS_KEY
#       remoteRef:
#         key: secret/data/minitoolstream/minio
#         property: secret_access_key
